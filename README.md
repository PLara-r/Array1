# Два массива (один как пример чтения и записи в массив, второй массив пример сортировки)
Очень часто используется цикл при чтении или записи в массив. Этот цикл устанавливает каждый элемент numberна 5 выше,
чем текущий индекс
